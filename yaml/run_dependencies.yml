name: 'news_clf_dependencies'
dependencies:
  - python=3.7.*
  - cudatoolkit=10.0
  - pip
  
  - pip:
    - azureml-defaults
    - azureml-sdk
    - numpy
    - pandas
    - logzero
    - tqdm
    - torch
    - nltk